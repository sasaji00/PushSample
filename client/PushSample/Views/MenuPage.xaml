<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="PushSample.Views.MenuPage"
             Title="{Binding Title}">

    <StackLayout HorizontalOptions="Fill" VerticalOptions="CenterAndExpand" Orientation="Vertical">
        <Button Text="プッシュ通知管理" Command="{Binding PushManagerCommand}"/>
        <Button Text="プッシュ通知登録" Command="{Binding PushRegisterCommand}"/>
    </StackLayout>

</ContentPage>